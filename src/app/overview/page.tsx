"use client";

import type React from "react";

const Homepage = (): React.JSX.Element => {
	return (
		<div className="container pt-8 mx-auto">
			<h1 className="text-4xl font-bold">Welcome back Pioneer!</h1>
		</div>
	);
};

export default Homepage;
